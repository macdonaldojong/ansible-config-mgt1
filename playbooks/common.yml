---
- name: update web, nfs and db webserver
  hosts: webserver, nfs
  remote_user: ec2-user
  become: yes
  become_user: root
  tasks:
    - name: ensure wireshark is at the latest version
      yum:
        name: wireshark
        state: latest

    - name:  set timezone
      timezone:
        name: Europe/Brussels

    - name: ensure wireshark is at the latest version
      yum:
        path: /home/sample/ansible.text
        state: touch

- name: update db server
  hosts: db
  remote_user: ubuntu
  become: yes
  become_user: root
  tasks:
    - name: Update apt repo
      apt: 
        update_cache: yes

    - name: ensure wireshark is at the latest version
      apt:
        path: /home/sample/ansible.text
        state: touch

    - name: ensure wireshark is at the latest version
      apt:
        name: wireshark
        state: latest
